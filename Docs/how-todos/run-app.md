# Run Project

## User service:

1. Setup virtual env:
`python3 -m virtualenv -p 3.10 venv`

2. Install python dependecies:
`pip3 install -r requirement.txt `

3. Run user API server:
`python3 user_service.py`
